language: php

php:
  - 5.5
  - 7.0
  - hhvm

sudo: false

cache:
  directories:
    - $HOME/.composer/cache

env:
  global:
    - SYMFONY_DEPRECATIONS_HELPER=strict

matrix:
  fast_finish: true
  allow_failures:
    - php: hhvm

before_install:
  - composer selfupdate

install:
  - composer update --prefer-dist --no-interaction

script:
  - phpunit
